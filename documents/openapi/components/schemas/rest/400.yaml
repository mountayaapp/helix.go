x-go-type: errorstack.Error
x-go-type-import:
  path: github.com/mountayaapp/helix.go/errorstack

allOf:
  - $ref: ../Errors.yaml

example:
  errors:
    - message: Request is invalid
      extensions:
        code: BAD_REQUEST
