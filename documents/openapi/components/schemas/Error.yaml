type: object
x-go-type: errorstack.Entry
x-go-type-import:
  path: github.com/mountayaapp/helix.go/errorstack

properties:
  message:
    type: string
    example: Must be a valid email address
  path:
    type: array
    items:
      oneOf:
        - type: string
        - type: integer
    example:
      - request
      - body
      - email
  extensions:
    type: object
    properties:
      code:
        type: string
        description: Machine-readable error code identifying the failure category.
        example: VALIDATION_FAILED
    required:
      - code

required:
  - message
  - extensions
